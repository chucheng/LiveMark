#!/usr/bin/env bash
cd "$(dirname "$0")"

if [ -n "$1" ]; then
  # Open a specific file with LiveMark
  filepath="$(cd "$(dirname "$1")" && pwd)/$(basename "$1")"
  pnpm tauri dev -- -- "$filepath"
else
  pnpm tauri dev
fi
