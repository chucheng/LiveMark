#!/usr/bin/env bash
cd "$(dirname "$0")"

if [ -n "$1" ]; then
  # Resolve all file arguments to absolute paths
  args=()
  for f in "$@"; do
    args+=("$(cd "$(dirname "$f")" && pwd)/$(basename "$f")")
  done
  pnpm tauri dev -- -- "${args[@]}"
else
  pnpm tauri dev
fi
